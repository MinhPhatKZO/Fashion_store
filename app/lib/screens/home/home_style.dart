import 'package:flutter/material.dart';

// Home screen specific style tokens
const Color kPrimaryColor = Color(0xFF40BFFF);
const double kCardRadius = 12.0;

BorderRadius get kCardBorderRadius => BorderRadius.circular(kCardRadius);

// Add more text styles or shadows here as needed for the home screen.
const TextStyle kSectionTitleStyle = TextStyle(
	fontSize: 18,
	fontWeight: FontWeight.bold,
);

const TextStyle kSectionSubtitleStyle = TextStyle(
	fontSize: 14,
	color: Colors.grey,
);
